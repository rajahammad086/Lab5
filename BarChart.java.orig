//Raja Hammad Mehmood
//This program would make bars of random heights with some space in between and the tallest bar should touch the top.
import java.awt.*;
import javax.swing.*;
public class BarChart {

  public static void main ( String[] args ) {
  Paint.buildWindow("Raja Hammad ", 0, 0, 1000, 1000, Color.WHITE);//back ground
  Paint.setColor(Color.RED);
  int y=0;// y is a variable used for the position of height
      for ( int x = 0; x <= 1000; x += 150) {
            Paint.fillRect(x,y,150,1000);
            x=x+20;
            y=(int)(Math.random()*1000)+1;// generating a random value for the position of height
        } 
     
     
     
     
   
  }
  
}